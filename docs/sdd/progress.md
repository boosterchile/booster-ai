# Eco-routing SDD progress

Task 1: complete (commits 6ab9829..f5fda24, review clean — spec OK, coverage exclusion fixed -> 100%, 7/7 tests)
Task 2: complete (commits f5fda24..0d5e34b, review Approved -- carbon-calc integration verified, zod->deps fixed). MINORS for final review: (a) fuelType z.string vs z.enum(TipoCombustible) silent-NaN risk for unknown fuel; (b) test mock metodo:string not literal modelado; (c) cargaKg:0 hardcoded undocumented in null-path estimate.
Task 3: complete (commit 9b06d70, review Approved -- 3-rep alignment exact across 14 cols, migration 0046 expand-only + .down.sql + journal OK, 15 TDD tests). MINORS for final review: M-1 lat/lng schema accepts empty string (Number("")=0, add .min(1)); M-3 coverage output not shown (verify route-suggestion.ts >=80% via CI); M-4 delta_co2e_kg z.string() no numeric regex.
Task 4: complete (commits 9b06d70..9ed2baa, review Approved -- fire-and-forget verified, en_proceso gating confirmed at assignments.ts:447-449 (409 if not asignado/recogido), Important schema->shared-schemas fixed = driverPositionEventSchema canonical). MINOR for final: registradoEn from request body (Task 5 must handle string datetime).
Task 4b (prep, discovered -- not in original plan): complete (commit 3ed4b87, behavior-preserving move). Extracted apps/api/src/services/routes-api.ts -> packages/routes-api-client (@booster-ai/routes-api-client). 11/11 package tests + 1564 api tests pass, typecheck 0. 5 api src files + 4 tests reimport from the package. Needed because apps cannot import from other apps (eco-routing-service reuses computeRoutes). Note: package tsconfig has no rootDir (TS6059 w/ logger source, matches whatsapp-client).
Task 5: complete (commits 3ed4b87..df83a59, review Approved -- no-crash consumer + Zod boundaries + store cooldown + orchestration-only verified, 24/24 tests ~91% coverage). HARD PREREQS FOR TASK 6 (reviewer Important, deferred by design): (P1) baseline uses PLACEHOLDER destination (origin=dest -> ~0); Task 6 MUST read the real trip destination + planned route (DB read of destino/ecoRoutePolylineEncoded) AND fix the necesitaBaseline guard (treat <=0 as needs-recompute, else in-flight trips keep poisoned ~0 baseline -> detector false-positive storm) BEFORE connecting traffic-condition-detector. (P2) consumer does NOT filter en_proceso (trusts Task-4 publisher gate assignments.ts:447-449); Task 6 add consumer-side filter. MINORS: registrarSugerencia synthetic (0,0) posicion; telemetry-events test thin. TripStateStore iface: getEstado/setPosicion/setBaseline/registrarSugerencia/puedeSugerir.
Task 6: complete (commits df83a59..994f044, review Approved -- P1 real destination + <=0 guard fixed, P2 en_proceso filter added, evaluarReruteo TDD 35/35 coverage 85.2%, 2 Important findings fixed: SQL params directs + narrowing recomendada). MINORS for final review: (a) tripData passed by value to debounced closure (stale if destination changes mid-trip, benign for trips); (b) missing consumer test for non-en_proceso estado path; (c) LEFT JOIN on asignaciones has no ORDER BY — arbitrary row if trip ever re-assigned.
Task 6 FINAL REVIEW: DONE_WITH_CONCERNS. C-1: trips.status='en_proceso' never set by any code (pre-existing gap from trip-state-machine). I-1: segundosHastaProximaDivergencia hardcoded=300. I-2: baseline uses live position not planned polyline. See task-6-report.md ##Final whole-branch review.
Task 6: DONE_WITH_CONCERNS, NOT YET REVIEWED (commits df83a59..994f044: trip-data-reader, P1/P2 fix, evaluar-reruteo, consumer wiring, SQL fix; 35/35 tests, 85.2% coverage, typecheck clean). CONCERNS escalated to PO: C-1 CRITICAL = trips.status never set to en_proceso anywhere (pre-existing platform gap) -> the en_proceso gate makes evaluarReruteo UNREACHABLE; fix = gate on assignment.status(asignado/recogido) like the publisher, OR fix trip-state pickup transition. I-1 = lead-time hardcoded 300 (geometric divergence not wired). I-2 = baseline computed from live position not planned route; spec baseline-at-start is incomparable w/ remaining-route live ETA; recommend static-vs-traffic duration of remaining route. AWAITING PO decision on C-1 gate + I-2 baseline semantics before finalizing Task 6 + Tasks 7-9.

PO DECISIONS (escalation resolved): C-1 -> "Arreglar transicion a en_proceso" (fix the real trip-state-machine transition at pickup: assignment->recogido should set trip->en_proceso; do NOT change the eco-routing gate). I-2 -> "Baseline de ruta planificada al inicio" (spec literal: compute baseline once at trip start for the planned route; revise Task 6 which used live-position baseline). I-1 -> accept as pilot simplification (geometric divergence = follow-up).
NEXT (resume map): (a) C-1 fix [feat(trips): transiciona viaje a en_proceso en el pickup] -- subagent errored 500 mid-investigation, NO changes left, re-dispatch; (b) I-2 revise baseline in eco-routing-service eval loop; (c) then REVIEW Task 6 (now reachable + correct baseline); (d) Tasks 7 (voice delivery), 8 (adoption-resolver), 9 (deploy -- owner-gated).

C-1 SCOPE DISCOVERY (inline investigation, API too overloaded for subagents): the ENTIRE pickup-confirmation flow is UNWIRED -- NO code sets assignment.status=recogido / recogido_en, NOR trips.status=en_proceso. Schema models recogido/en_proceso + recogido_en timestamp; no pickup-confirmation handler exists. trip-state-machine VALIDATES asignado->en_proceso (transiciones.ts:26). So PO choice "fix the transition" actually = BUILD the pickup-confirmation feature (driver confirms pickup -> assignment recogido + trip en_proceso), beyond eco-routing scope, needs PO product scoping (UX, validations, endpoint). RE-ESCALATED to PO: (A) build pickup flow now (new feature) vs (B) interim: gate eco-routing on assignment.status=asignado (functional now; pickup flow = separate backlog).
