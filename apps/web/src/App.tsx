/**
 * @deprecated B.3.b — reemplazado por router programático en `src/router.tsx`.
 *
 * Este archivo era el placeholder pre-router. `main.tsx` ya no lo importa;
 * existe sólo porque el sandbox no permite borrar archivos. Se eliminará
 * en un commit posterior cuando se haga cleanup del workspace.
 */
export {};
